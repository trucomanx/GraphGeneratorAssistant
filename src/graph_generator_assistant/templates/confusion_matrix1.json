{
    "title": "Confusion matrix",
    "description": "We have as input a confusion matrix **conf_mat** where the lines are the true values and the columns are the predicted values, and a string list called **target_names** with the labels of categories (ex. ['A','B','C'])."
}
