{
    "title": "Scatter relationship plot",
    "description": "The x-axis values are stored in the **x_data** variable (list of numbers). The y-axis values are stored in the **y_data** variable (list of numbers). The function creates a scatter plot with optional regression line, configurable point size, color, transparency, and labels."
}

