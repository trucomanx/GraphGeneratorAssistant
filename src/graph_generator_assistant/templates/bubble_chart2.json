{
    "title": "Bubble chart",
    "description": "The category labels are stored in the **labels** variable. The x-axis values are stored in the **x_data** variable. The y-axis values are stored in the **y_data** variable. The bubble size and color values are stored in the **z_data** variable."
}

