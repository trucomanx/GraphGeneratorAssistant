{
    "title": "Radial network",
    "description": "We call **Labeled Node** to each element in the workflow, the input is a list of labels (strings). We call **Central Node** to the central element in the workflow, the input is a string."
}
