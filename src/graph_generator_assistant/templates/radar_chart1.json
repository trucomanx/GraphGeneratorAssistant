{
    "title": "Radar chart",
    "description": "The radar chart data is stored in the **data** variable. Each top-level key represents a data series, while the nested keys define the radar chart categories. The associated numeric values represent the 0 to 100 percentage scores plotted along the radial axis."
}

